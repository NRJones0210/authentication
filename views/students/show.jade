if errors
  include ../nav_notLoggedIn
    .container
      .alert.alert-danger
        for error in errors
          li= error
if !errors
  include ../nav_loggedIn
    .container
      .row.text-center
        h1= title
      .row
        .col-md-3
        .col-md-6
          h3 Name: #{student.name}
          h3 Phone: #{student.phone}

include ../footer

